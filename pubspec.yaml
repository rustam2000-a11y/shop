name: shop
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  firebase_core: ^3.6.0 # Обновленная версия firebase_core
  firebase_auth: ^5.3.1 # Совместимая версия firebase_auth
  cloud_firestore: ^5.4.4 # Совместимая версия cloud_firestore
  email_validator: ^2.1.17 # Для валидации email-адресов
  firebase_storage: ^12.3.3
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
